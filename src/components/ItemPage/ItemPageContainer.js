// import React from 'react';
// import { connect } from 'react-redux';
import ItemPageLayout from './ItemPageLayout';

// const mapStateToProps = function(store) {
//
//     let searchType = store.ItemPageLayoutState.searchType;
//     let totalResults = 0;
//
//     if (searchType === 'users') {
//         totalResults = store.userState.users.length;
//     } else if (searchType === 'widgets') {
//         totalResults = store.widgetState.widgets.length;
//     }
//
//     return {
//         searchType,
//         title: store.ItemPageLayoutState.title,
//         totalResults
//     };
//
// };
//
// export default connect(mapStateToProps)(ItemPageLayout);

export default ItemPageLayout;